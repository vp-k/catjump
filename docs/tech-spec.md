# Cat Jump: Tower Stack - 기술 명세서

**프로젝트 기술 스택 및 아키텍처 문서**

---

## 📋 문서 정보

- **버전**: 1.2
- **최종 수정**: 2025-11-29
- **관련 문서**: [game-design.md](./game-design.md) (v1.6.2 동기화)
- **v1.2 변경**: 심리엔진, FTUE, 리텐션, COPPA/GDPR, FCM, 성능예산 반영

---

## 🛠️ 기술 스택 상세

### 1. 프론트엔드 / 게임 엔진

#### Phaser 3
- **버전**: 3.80+ (최신 버전)
- **렌더러**: WebGL + Canvas fallback
- **선택 이유**:
  - 2D 게임에 최적화
  - 물리 엔진 내장 (Arcade Physics)
  - TypeScript 지원
  - 활발한 커뮤니티
  - **특수 캔 구현 용이**: setTint(), setScale(), tweens 활용

#### TypeScript
- **버전**: 5.0+
- **선택 이유**:
  - 타입 안정성
  - 코드 자동완성
  - 리팩토링 용이
  - 버그 사전 방지

#### Vite
- **버전**: 5.0+
- **선택 이유**:
  - 빠른 HMR (Hot Module Replacement)
  - 빠른 빌드 속도
  - TypeScript 기본 지원
  - 간단한 설정

---

### 2. 모바일 래핑

#### Capacitor

**선택 이유 (vs React Native)**:

| 기준 | Capacitor ✅ | React Native |
|------|-------------|--------------|
| **Phaser 통합** | 네이티브 WebView | 복잡한 브릿지 |
| **개발 속도** | 빠름 | 느림 |
| **학습 곡선** | 낮음 | 높음 |
| **성능 (2D)** | 충분 | 더 좋음 |
| **코드 공유** | 100% | 60-80% |
| **검증** | 브라우저 → 앱 | 시뮬레이터 필요 |
| **빌드 크기** | 작음 | 큼 |

**결론**: 2D 캐주얼 게임에는 Capacitor가 최선

**플러그인**:
- `@capacitor/app` - 앱 라이프사이클
- `@capacitor/splash-screen` - 스플래시
- `@capacitor/status-bar` - 상태바
- `@capacitor/haptics` - 진동 피드백
- `@capacitor/share` - 공유 기능

---

### 3. 백엔드 / 서비스

#### Firebase
- **Authentication**: 익명 로그인, Google 로그인
- **Firestore**:
  - 유저 데이터 저장
  - 리더보드
  - 업적 진행도
  - 미션 데이터 (일일 3개, 주간 7개)
- **Cloud Functions**:
  - 리더보드 업데이트
  - 데이터 검증 (validateScore)
  - 보상 지급 (grantReward)
  - 리텐션 훅 처리
- **Cloud Messaging (FCM)**: 푸시 알림
  - D1 리텐션 훅 (2시간/4시간/취침 전)
  - D3-D5 재방문 유도
  - D7-D14 복귀 유도
- **Analytics**: 게임 이벤트 추적
- **Remote Config**: A/B 테스트, 기능 플래그

#### AdMob

- **Rewarded Ads**: 보상형 광고 (부활, 보상 2배)
- **Interstitial Ads**: 전면 광고 (5게임당 1회, 주의 피로도 관리)
- **SDK**: Firebase와 통합
- **COPPA/GDPR**: 연령 확인 + 동의 UI 필수

#### In-App Purchase
- **Android**: Google Play Billing
- **iOS**: StoreKit 2
- **플러그인**: `@capacitor-community/in-app-purchases`
- **보호자 게이트**: 만 13세 미만 구매 시 보호자 확인
- **퍼널 추적**: 상점 진입 → 상품 조회 → 결제 시도 → 완료

---

## 🏗️ 프로젝트 구조

```
CATJUMP/
├── docs/                      # 📋 문서
│   ├── game-design.md        # 게임 기획서
│   ├── tech-spec.md          # 기술 명세서 (이 파일)
│   └── progress.md           # 진행 상황 체크리스트
│
├── src/                       # 💻 소스 코드
│   ├── index.html            # 메인 HTML
│   ├── main.ts               # 앱 진입점
│   │
│   ├── scenes/               # 🎬 Phaser 씬
│   │   ├── BootScene.ts
│   │   ├── PreloadScene.ts
│   │   ├── MenuScene.ts
│   │   ├── GameScene.ts
│   │   ├── GameOverScene.ts
│   │   └── HouseScene.ts
│   │
│   ├── entities/             # 🎮 게임 오브젝트
│   │   ├── Cat.ts            # 감정 상태 머신 포함
│   │   ├── Can.ts            # 기본캔 + 특수캔 (황금/넓은/함정/선물/흔들)
│   │   ├── SnackTower.ts     # 콤보 간식 탑 시각화 (손실 회피)
│   │   └── Coin.ts
│   │
│   ├── managers/             # 🔧 매니저
│   │   ├── ScoreManager.ts
│   │   ├── ComboManager.ts   # 콤보 시스템 + 배율 계산
│   │   ├── DifficultyManager.ts  # 난이도 곡선 관리
│   │   ├── PsychologyManager.ts  # 심리 엔진 (Near-Miss, Mercy)
│   │   ├── TutorialManager.ts    # FTUE 4단계
│   │   ├── MissionManager.ts     # 일일/주간 미션
│   │   ├── RetentionManager.ts   # 리텐션 훅 + 스트릭
│   │   ├── SoundManager.ts
│   │   ├── SaveManager.ts
│   │   ├── AdManager.ts      # 광고 노출 제어 포함
│   │   └── IAPManager.ts     # 퍼널 추적 포함
│   │
│   ├── ui/                   # 🎨 UI 컴포넌트
│   │   ├── Button.ts
│   │   ├── ScoreDisplay.ts
│   │   └── ProgressBar.ts
│   │
│   ├── config/               # ⚙️ 설정
│   │   ├── GameConfig.ts
│   │   └── AssetConfig.ts
│   │
│   ├── utils/                # 🛠️ 유틸리티
│   │   ├── Firebase.ts
│   │   └── Analytics.ts
│   │
│   └── assets/               # 🎨 에셋
│       ├── images/
│       ├── audio/
│       └── fonts/
│
├── android/                   # 📱 Android (Capacitor)
├── ios/                       # 🍎 iOS (Capacitor)
│
├── capacitor.config.ts       # Capacitor 설정
├── vite.config.ts            # Vite 설정
├── tsconfig.json             # TypeScript 설정
└── package.json              # 패키지 정보
```

---

## 🎮 코어 시스템 설계

### 1. Scene 구조

```typescript
BootScene
  ↓
PreloadScene (로딩)
  ↓
MenuScene ←→ GameScene ←→ GameOverScene
  ↓             ↓
HouseScene    (pause)
```

### 2. 게임 플레이 루프

```
GameScene.create()
  ↓
setupPhysics()
  ↓
spawnCat()
spawnCan()
  ↓
update() Loop:
  ├─ handleInput()
  ├─ catPhysics()
  ├─ cameraFollow()
  ├─ checkCollision()
  ├─ updateScore()
  ├─ updateCombo()        # 콤보 + 간식 탑 업데이트
  ├─ spawnCan()           # 특수 캔 포함
  └─ checkHighScore()     # Near Miss 알림
  ↓
gameOver() → GameOverScene
```

### 3. 물리 시스템

**Phaser Arcade Physics**:
- Gravity: 800 (적당한 점프감)
- Cat Jump Velocity: -500
- Can Static Bodies
- Obstacle Dynamic Bodies

### 4. 데이터 저장

**LocalStorage** (오프라인):
```typescript
{
  coins: number,
  diamonds: number,
  highScore: number,
  unlockedCats: string[],
  unlockedCostumes: string[],
  currentCat: string,
  settings: {
    sound: boolean,
    music: boolean,
    haptics: boolean
  }
}
```

**Firestore** (온라인):
```typescript
users/{uid}/
  ├─ profile: { name, level, exp }
  ├─ stats: { gamesPlayed, highScore }
  ├─ inventory: { cats, costumes }
  └─ achievements: { [id]: progress }

leaderboards/global/
  └─ scores/{uid}: { score, timestamp }
```

---

## 📱 모바일 최적화

### 성능
1. **Object Pooling**: 캔, 코인, 간식 탑 재사용
2. **Texture Atlas**: 모든 스프라이트 단일 파일
3. **Audio Sprite**: 사운드 하나로 통합
4. **Lazy Loading**: 필요한 에셋만 로드
5. **FPS 제한**: 60fps 고정
6. **Tween 최적화**: 특수 캔 효과는 가벼운 트윈 사용

### 디바이스 호환
- **해상도**: 720p ~ 1080p 대응
- **Safe Area**: 노치/홈바 대응
- **터치**: 멀티 터치 지원
- **방향**: 세로 고정

---

## 🔐 보안

1. **API Key**: 환경 변수로 관리
2. **Cloud Functions**: 서버 검증
3. **Firestore Rules**: 데이터 접근 제한
4. **난독화**: 프로덕션 빌드 시

---

## 🧪 테스트 전략

### 단위 테스트
- Vitest
- 매니저 클래스 테스트
- 유틸 함수 테스트

### 통합 테스트
- 브라우저 자동 테스트
- Playwright 사용

### 수동 테스트
- 다양한 기기 테스트
- 네트워크 환경 테스트
- 광고 통합 테스트

---

## 📦 빌드 & 배포

### 개발 환경
```bash
npm run dev
```

### 프로덕션 빌드
```bash
npm run build
npm run preview
```

### 모바일 빌드
```bash
# Android
npx cap sync android
npx cap open android

# iOS
npx cap sync ios
npx cap open ios
```

---

## 📈 분석 & 모니터링

### Firebase Analytics
**커스텀 이벤트**:
- `game_start`
- `game_over` (score, floor)
- `perfect_landing`
- `obstacle_hit`
- `coin_collected`
- `ad_watched`
- `iap_purchased`
- `cat_unlocked`

### 성능 모니터링
- Firebase Performance
- FPS 트래킹
- 로딩 시간 측정

---

## 🔄 업데이트 전략

### OTA (Over-The-Air) Updates
- **Firebase Remote Config**: 
  - 게임 밸런스 조정
  - 이벤트 ON/OFF
  - 광고 빈도 조정

### 앱 업데이트
- **Major Update**: 새 기능, 큰 변경
- **Minor Update**: 버그 수정, 밸런스

---

## 🎯 성능 목표

| 지표 | 목표 |
|------|------|
| FPS | 60fps 유지 |
| 로딩 시간 | < 3초 |
| 앱 크기 | < 50MB |
| 메모리 | < 200MB |
| 배터리 | 최소 사용 |

---

## 🧠 심리 엔진 시스템 (v1.6 추가)

### 감정이입 시스템

- 고양이 감정 상태 머신 (idle, happy, excited, worried, sad)
- 상태별 표정/애니메이션 변화
- 콤보 상태에 따른 감정 연동

### 손실 회피 시스템

- 콤보 간식 탑 시각화 (쌓임 → 붕괴)
- Good 착지 시 흔들림 경고
- 콤보 끊김 시 붕괴 이펙트

### Mercy 시스템

- 연속 3회 실패 시 Perfect 존 20% 확대
- 5회 연속 실패 시 캔 속도 15% 감소
- 복귀 시 점진적 정상화

### Near-Miss 시스템

- 1px 차이 감지 시 슬로우 모션 + 강조
- 최고기록 -1/-2/-3층 근접 알림
- 메달 근접 감지 (브론즈 25층 근처 등)

---

## 📱 FTUE (튜토리얼) 시스템 (v1.4 추가)

### 4단계 Wave 구조

1. **Wave 1**: 탭으로 점프 (1-2층)
2. **Wave 2**: Perfect 존 강조 (3-4층)
3. **Wave 3**: 콤보 소개 (5층)
4. **Wave 4**: 특수 캔 안내 (10층)

### 자동 스킵 로직

- 5층 자력 도달 시 튜토리얼 완료 처리
- 재방문 유저는 스킵 가능

---

## 🔁 리텐션 시스템 (v1.6 추가)

### 일일 로그인 보상

- 7일 주기 보상 사이클
- 스트릭 보호/복구 시스템

### 푸시 알림 (FCM)

| 시점 | 메시지 |
|------|--------|
| D1 +2h | "고양이가 배고파해요!" |
| D1 +4h | "간식탑이 무너질 것 같아요..." |
| D3 | "최고기록 깰 준비 됐나요?" |
| D7 | "일주일 만이에요! 특별 보상 대기 중" |

### FOMO 시스템

- 한정 이벤트 타이머
- 일일 미션 만료 카운트다운
- 스트릭 위험 알림

---

## ⚖️ 법적 준수 (v1.4 추가)

### COPPA/GDPR

- 연령 확인 UI (최초 실행 시)
- 만 13세 미만: 개인화 광고 비활성화
- 데이터 수집 동의 UI
- 보호자 게이트 (IAP)

### 확률 투명성 (v1.6.2)

- 선물캔/가챠 확률 공개
- 상점 내 확률표 링크

---

**기술 명세서 v1.2 완성** ✅
